# Session: Sprint 1.5 — README Honesty + Homelab Deployment
Date: 2026-04-17

## What was completed
- PR #5 — `docs(readme): strip unimplemented security claims, document actual state` (merged to main)

## What changed
- `README.md` rewritten. Removed:
  - Outbound WebSocket transport (agent uses HTTP long-polling today)
  - 8-layer security model (Layers 2 / 7 / 8 are not implemented)
  - mTLS (no `tls.rs`, no cert handling)
  - Signed command verification (no `sha2`/`ring` in `Cargo.toml`)
  - Namespace / cgroup sandbox (`sandbox.rs` does not exist)
  - 24-hour token-rotation claim (rotation happens per-heartbeat)
  - Installer URL `get.sigops.dev/agent`
  - Docker image on GHCR
  - `pm2` / `kubectl` executor adapters
  - Nested module-layout diagram (actual layout is flat)
- Added `## Roadmap` section capturing all of the above.
- Replaced the module-layout block with the real layout (`config.rs`, `discovery.rs`, `heartbeat.rs`, `executor.rs`, `security.rs`, `health.rs`, `main.rs`, `lib.rs`).

## Decisions made
- Roadmap is the canonical home for "we said it publicly but haven't built it" items — not the main body of the README.
- Security-claim precision: if a customer can't grep it in `src/`, it doesn't ship in the README.

## What's pending
- `CLAUDE.md` still documents the 8-layer model and the nested `ws/`, `tools/`, `security/sandbox.rs`, etc. layout. Not rewritten in this sprint to avoid a large spec edit; flagged for a follow-up.
- `SECURITY.md` and `CONTRIBUTING.md` referenced by the README are still missing. Out of scope for this sprint.
- No release has been cut; release workflow exists but has never fired. Needs a `v0.1.0` tag.

## Next steps
1. Align `CLAUDE.md` with the real module layout and current security model.
2. Cut `v0.1.0` to publish cross-compiled binaries via the existing `release.yml`.
3. Add `cargo audit` + SBOM + code-signing to the release workflow.
4. Start the WSS transport / mTLS / signed-commands tracks (in that order).
